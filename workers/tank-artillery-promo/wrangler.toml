# Deploy: cd workers/tank-artillery-promo && wrangler kv namespace create PROMO — copy id into wrangler.toml — wrangler deploy
name = "tank-artillery-promo"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[[kv_namespaces]]
binding = "PROMO"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
